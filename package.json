{
  "name": "news-fetcher",
  "version": "1.0.0",
  "description": "Daily news fetcher service",
  "main": "api/fetch-news.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel deploy",
    "deploy:prod": "vercel deploy --prod"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.2",
    "axios": "^1.6.2",
    "news-fetcher": "file:"
  },
  "devDependencies": {
    "vercel": "^32.7.2"
  }
}
